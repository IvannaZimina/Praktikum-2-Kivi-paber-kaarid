<Window x:Class="RpsTournament.WpfApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Kivi-Paber-Käärid Turniir" Height="470" Width="520" WindowStartupLocation="CenterScreen"
        Background="#F4F5F7">

    <Window.Resources>
        <Style TargetType="TextBox">
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="8,0"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="BorderBrush" Value="#D0D5DD"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="White" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="34"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#E2E8F0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="RowHeight" Value="28"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="HorizontalGridLinesBrush" Value="#F1F5F9"/>
            <Setter Property="VerticalGridLinesBrush" Value="Transparent"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="ColumnHeaderStyle">
                <Setter.Value>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#F8FAFC"/>
                        <Setter Property="Foreground" Value="#475569"/>
                        <Setter Property="FontWeight" Value="Bold"/>
                        <Setter Property="Padding" Value="10,0"/>
                        <Setter Property="HorizontalContentAlignment" Value="Center"/>
                        <Setter Property="BorderBrush" Value="#E2E8F0"/>
                        <Setter Property="BorderThickness" Value="0,0,0,1"/>
                    </Style>
                </Setter.Value>
            </Setter>
            <Setter Property="CellStyle">
                <Setter.Value>
                    <Style TargetType="DataGridCell">
                        <Setter Property="Padding" Value="10,0"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="DataGridCell">
                                    <Border Background="{TemplateBinding Background}" BorderBrush="Transparent" BorderThickness="0">
                                        <ContentPresenter VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="18,22,18,18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="105"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="110"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" VerticalAlignment="Center" FontWeight="SemiBold" FontSize="14" Foreground="#333333">
                <Run Text="Mängija nimi"/><Run Text=" *" Foreground="#D9534F" FontWeight="Bold"/>
            </TextBlock>
            <TextBox x:Name="PlayerNameTextBox" Grid.Column="1" Height="32" Margin="0,0,10,0" TextChanged="PlayerNameTextBox_TextChanged"/>
            <Button x:Name="PlayRoundButton" Grid.Column="2" Content="Mängi voor" Style="{StaticResource ActionButtonStyle}" Background="#0284C7" Click="PlayRoundButton_Click"/>
        </Grid>

        <TextBlock x:Name="StatusTextBlock" Grid.Row="1" Foreground="#D9534F" FontWeight="SemiBold" FontSize="12" Margin="105,3,10,6"/>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="105"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="110"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" VerticalAlignment="Center" FontWeight="SemiBold" FontSize="14" Foreground="#333333">
                <Run Text="Vali käik"/><Run Text=" *" Foreground="#D9534F" FontWeight="Bold"/>
            </TextBlock>

            <Grid Grid.Column="1" Margin="0,0,10,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Button x:Name="RockButton" Grid.Column="0" Content="Kivi" Click="MoveButton_Click" Tag="Rock" Height="34" Margin="0,0,4,0" Foreground="White" FontWeight="SemiBold" Cursor="Hand" Background="#1D4ED8">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" CornerRadius="6">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>

                <Button x:Name="PaperButton" Grid.Column="1" Content="Paber" Click="MoveButton_Click" Tag="Paper" Height="34" Margin="2,0,2,0" Foreground="White" FontWeight="SemiBold" Cursor="Hand" Background="#2563EB">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" CornerRadius="6">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>

                <Button x:Name="ScissorsButton" Grid.Column="2" Content="Käärid" Click="MoveButton_Click" Tag="Scissors" Height="34" Margin="4,0,0,0" Foreground="White" FontWeight="SemiBold" Cursor="Hand" Background="#3B82F6">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" CornerRadius="6">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </Grid>

            <Button x:Name="NewTournamentButton" Grid.Column="2" Content="Uus turniir" Style="{StaticResource ActionButtonStyle}" Background="#64748B" Click="NewTournamentButton_Click"/>
        </Grid>

        <TextBlock Grid.Row="3" Foreground="#64748B" FontSize="11" Margin="0,6,0,10">
            <Run Text="*" Foreground="#D9534F" FontWeight="Bold"/><Run Text=" - kohustuslikud väljad"/>
        </TextBlock>

        <Border Grid.Row="4" Background="White" Padding="10" CornerRadius="8" Margin="0,0,0,10" BorderBrush="#E2E8F0" BorderThickness="1">
            <TextBlock x:Name="ScoreTextBlock" Text="Võidud: 0 | Kaotused: 0 | Viigid: 0" FontSize="14" FontWeight="Bold" Foreground="#1E293B" HorizontalAlignment="Center"/>
        </Border>

        <DataGrid x:Name="RoundsDataGrid" Grid.Row="5" AutoGenerateColumns="True" IsReadOnly="True" 
                  AutoGeneratingColumn="RoundsDataGrid_AutoGeneratingColumn" Margin="0,10,0,45"/>
    </Grid>
</Window>